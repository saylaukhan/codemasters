# Известные ограничения

Это список того, что заведомо не доделано. Он нужен, чтобы вы или ИИ-агент не приняли
недоделанное за баг и не полезли чинить то, что решено оставить как есть. Это не план работ:
планы — в `docs/tasks/README.md`, решения — в `docs/architecture/decisions/`.

| Ограничение | Влияние | Что делать |
|---|---|---|
| Есть только каркас (T-01), схема БД v1 (T-02), контракт API всех эндпоинтов plan.md §10 (T-03), `make seed` со справочниками и тестовыми школами (T-04) и сервер замеров LibreSpeed + ndt7 в compose (T-05), служба агента с конфигом и CLI `install` / `uninstall` / `run` / `status` без цикла замеров (T-06), регистрация агента по коду установки с хранением токена (T-07); функции ТЗ не реализованы | ни одна функция ТЗ не работает; эндпоинты в `backend/app/api/` — заглушки, отвечают 501 `not_implemented` с номером задачи реализации; токен устройства и JWT только объявлены в OpenAPI, не проверяются; папки внутри `agent/internal/`, `web/src/pages/` в документах — план, а не факт | реализация — задача из ответа 501; проверка токенов — T-14, T-20; статусы задач — `docs/tasks/README.md` |
| Эндпоинтов, которых нет в plan.md §10, в контракте нет, хотя задачи их называют: полигоны районов для карты (T-04, T-22), справочники районов, провайдеров и типов подключения для фильтров всех ролей — сейчас только в `/api/admin` (T-22, T-24), список устройств, перепривязка устройства к точке, ротация токена (T-36), точки мониторинга (T-35), замеры школы для вкладки карточки (T-25), список и карточка обращений (T-48), список выгрузок (T-33), журнал уведомлений (T-39), выбор ответственного за инцидент (T-41), загрузка MSI релиза (T-50) (T-03) | панель не получит полигоны районов и не сможет управлять точками и устройствами, пока эндпоинт не добавлен | решает лид; эндпоинт добавляет задача, которой он нужен, — правкой схем и `make openapi` в том же слиянии (ADR-009) |
| Для роли провайдера не определено, из какой линии берутся поля школы уровня «основная линия» (статус, средние, поставщик, договор, последний замер), если у провайдера только резервная линия школы (T-03) | без правила реализация может показать провайдеру данные чужой линии (ТЗ п. 16) или пустую карточку | T-20 и T-44: зафиксировать правило в описании полей контракта и тестом «провайдер не видит чужую линию» |
| `working_hours` школы есть в контракте (`SchoolDetail`, `SchoolUpdate`), но не в схеме БД (T-03) | рабочие часы нельзя сохранить; доступность и «Нет соединения» в рабочие часы (ADR-014) пока не на чем считать | T-37: хранение рабочих часов и реализация `PATCH /api/schools/{id}` для них |
| Аналитика (`GET /api/analytics`) отдаёт долю замеров ниже договора, а агрегаты `m_hourly` / `m_daily` по plan.md §5 её не содержат и Wi‑Fi не исключают (T-03) | T-27 не сможет прочитать долю ниже договора и средние без Wi‑Fi только из агрегатов | T-19: добавить в агрегаты число замеров ниже договора и исключение Wi‑Fi (ADR-012) |
| Пресеты аналитики «неделя» и «месяц» в контракте — скользящие 7 и 30 суток по Asia/Almaty; ТЗ п. 5 («текущий день, неделю, месяц») можно читать как календарные (T-03) | цифры за «месяц» в панели и в ожиданиях заказчика могут не совпасть | T-27: подтвердить у лида; меняется только описание параметра `period` |
| В контракте есть поля и данные, которых нет в схеме БД plan.md §5: `providers.appeals_email` (T-34, T-48), хранилище выгрузок и их файлов (T-30, T-33), канал релиза агента у устройства (T-50) (T-03) | реализация эндпоинта упрётся в отсутствующую колонку или таблицу | задача реализации добавляет миграцию в том же слиянии; канал устройства — решает T-50 |
| Идемпотентность простоев есть только в контракте: `POST /api/outages` отвечает 409 `duplicate_outage` для того же устройства и `started_at`, но уникального индекса в `outages` нет (T-03) | пока приём простоев не реализован, ничего не ломается; реализация без индекса создаст дубль простоя при повторной досылке | T-16: уникальный индекс `(device_id, started_at)` миграцией и ответ 409 |
| Невалидная запись в `POST /api/measurements/batch` отклоняет весь запрос с 422 (индекс записи — в `errors[].field`), а ADR-006 удаляет запись из очереди только после 201/409 (T-03); агент (T-11) после 422 на пакет отправляет его записи по одной, запись с 422 помечает `rejected` с текстом ошибки и больше не отправляет, но не удаляет | запись, которую сервер никогда не примет (например, после проверки диапазонов в T-51), не блокирует очередь, но лежит в `queue.db` до лимита 30 дней / 10 000 и на сервер не попадает; `vko-agent status` её не считает | решить с лидом, удалять ли такие записи сразу и передавать ли факт отказа на сервер; решение — правкой ADR-006 |
| Версии FastAPI и Pydantic в `backend/pyproject.toml` не закреплены, а тест `test_openapi_file_matches_the_code` сравнивает `docs/reference/openapi.json` с кодом дословно (T-03) | новая версия FastAPI или Pydantic в CI может изменить текст схемы, и `make check-backend` покраснеет без изменений в коде | `make backend-install`, `make openapi`, закоммитить; закрепить версии — решение лида (правка `pyproject.toml`) |
| `errors[].message` по-русски только для частых типов ошибок Pydantic; для остальных остаётся английский текст Pydantic (T-03) | редкая ошибка валидации покажется в форме панели по-английски | дополнять `VALIDATION_MESSAGES` в `backend/app/core/errors.py`, когда такая ошибка встретится |
| Уникальность `measurement_uuid` в БД — только в паре с `measured_at` (`uq_measurements_measurement_uuid`): TimescaleDB не разрешает на hypertable уникальный индекс без колонки партиционирования (T-02) | повтор замера с теми же `measurement_uuid` и `measured_at` база отклоняет (ADR-006); тот же `measurement_uuid` с другим `measured_at` база не поймает — при штатной досылке такого нет, агент не меняет запись в очереди | T-15: при приёме искать дубль по одному `measurement_uuid` и отвечать 409 |
| Тестовые школы `make seed` — список data.egov.kz на 27.04.2024 (356 из 354 школ ВКО на 2026): закрытые с тех пор школы остались, открытые в 2025–2026 не добавлены, 4 школы с координатами за границей ВКО исключены; провайдеры, типы подключения и договорные скорости линий вымышленные; полигоны районов упрощены примерно до 200 м (T-04) | на демо карта и списки показывают реальные названия школ с выдуманными линиями; школа у самой границы района в реальных данных может попасть в соседний полигон | коды школ и линии — от заказчика; что из seed нужно в production — T-57; подробности и правило линий — `backend/app/seed_data/README.md` |
| Таблица `settings` хранит только адреса серверов замеров (`librespeed_url`, `ndt7_url`); остальных полей контракта `SettingsDetail` в БД нет (T-05) | `GET/PATCH /api/admin/settings` нельзя реализовать без миграции; интервалы агента, пороги доступности и сроки не на чем хранить | задача, которой поле нужно (T-17, T-29, T-33, T-37, T-41), добавляет колонку новой миграцией с серверным значением по умолчанию для существующей строки |
| LibreSpeed и ndt7 в compose работают без TLS (`http://`, `ws://`), порты 8080 и 8081 открыты наружу напрямую, не через Caddy (T-05) | в бою трафик замера идёт открытым текстом; решение «HTTPS/TLS 1.2+ через Caddy» из `docs/tasks/README.md` описывает канал агента к API, про сервер замеров оно не говорит | решает лид: сертификат для speedtest и ndt7 (`-cert`/`-key`) или прокси через Caddy — до поставки, T-57 описывает итог |
| Образ `measurementlab/ndt-server` собран только для amd64 (T-05) | на Apple Silicon ndt7 работает в эмуляции: локальные цифры резервного замера занижены | не планируется: сервер замеров в бою — amd64 |
| Установка службы на Windows проверена только сборкой (`GOOS=windows go build`, `go vet`): `vko-agent install` не запускался на живом Windows; отложенный автозапуск, LocalService и перезапуск 1 мин / 1 мин / 5 мин не проверены в «Службы» (T-06) | на чистом Windows возможна ошибка регистрации или прав, которую тесты на macOS/Linux не ловят | T-49: проверка на чистом Windows вместе с MSI |
| Папку данных `%ProgramData%\VKO Monitor` агент создаёт сам; права на неё для LocalService не выставляются, журнал `agent.log` не ротируется (T-06) | если LocalService не сможет писать в папку, служба упадёт при старте и уйдёт в перезапуск; журнал растёт без ограничения | T-49: MSI создаёт папку с правами на запись для LocalService; ротация журнала — решает лид |
| На Linux unit systemd работает от root: отдельного пользователя нет (T-06) | нарушает «минимум прав» plan.md §4.1 на Linux; на Windows служба — LocalService | T-52: пакет создаёт пользователя `vko-agent`, unit — с `User=` |
| Регистрация агента проверена только на тестовом HTTP-сервере: `POST /api/devices/register` на backend отвечает 501, пока нет T-14; агент считает 5xx временной ошибкой и повторяет попытку с паузой от 30 с до 15 мин (T-07) | до T-14 агент в журнале пишет «регистрация не удалась, повтор позже»; сквозной путь «код → `device_id` → токен» не проверен | T-14: после реализации эндпоинта проверить `make agent-run` с кодом из `enrollment_codes` |
| DPAPI (`agent/internal/secure/dpapi_windows.go`) проверен только сборкой и `go vet` под Windows: тесты `secure` на Windows не запускались, `ci.yml` гоняет агента на ubuntu (T-07) | ошибка вызова DPAPI проявится только на Windows: токен не сохранится, служба останется без регистрации | T-49: прогнать `go test ./internal/secure/` на Windows вместе с проверкой MSI |
| На Linux и macOS токен устройства лежит в `device_token` открытым текстом с правами 600 (ADR-005); «в открытом виде не найти» выполняется только на Windows (T-07) | root или владелец файла прочитает токен | решает лид; на Linux — ключ из `/etc/machine-id` или keyring, если заказчику нужно |
| Токен зашифрован DPAPI в области учётной записи службы (LocalService): `vko-agent run` под другим пользователем токен не прочитает; `device_uid` — случайный и живёт в `device.json` папки данных (T-07) | запуск агента не от службы или удаление папки данных — нужна регистрация по новому коду, на сервере появится второе устройство | админка устройств — T-36 (блокировка старого); переход на MachineGuid — решает лид |
| Отказ сервера по коду (просрочен, использован, чужой) агент пишет в журнал один раз и больше не пытается до перезапуска службы; код из `agent.yaml` после регистрации не стирается (T-07) | после выдачи нового кода нужно поправить `enroll_code` и перезапустить службу | T-49: MSI перезаписывает код при переустановке; статус регистрации виден в `vko-agent status` |
| Планировщик `agent/internal/scheduler/` запускается службой по расписанию с сервера (T-13), но сквозь backend не проверялся: `GET /api/agent/config` отвечает 501 до T-17, проверено юнит-тестами на подменённых часах и на подменном HTTP-сервере; выход из сна на живом Windows не проверялся; из пропущенных за день слотов догоняется только последний начавшийся (plan.md §4.2) (T-08) | сон распознаётся по скачку настенных часов больше чем на 2 мин за минутное ожидание, поэтому перевод часов вперёд тоже даёт догоняющий замер | сквозная проверка расписания с сервера — после T-17; сон на Windows — T-49 |
| Проверка связи и серия ping идут из замера по расписанию (T-13) и из диагностической `vko-agent probe`; сервер замеров берётся из `GET /api/agent/config`, внешний IP — из `GET /api/agent/whoami` (оба 501 до T-17, поэтому сквозь backend не проверялось); проверка связи — `GET /api/health`, связь есть при любом HTTP-ответе (эндпоинта с ответом 204 на сервере нет); служба на Windows работает под LocalService, а raw ICMP в `pro-bing` требует прав администратора; тип адаптера и шлюз определяются только на Windows и Linux, на живом Windows не проверялись (T-09) | на Windows серия, скорее всего, всегда идёт через TCP-connect (метод `tcp`, RTT чуть больше, чем у ICMP) | ICMP без прав администратора — `IcmpSendEcho` из iphlpapi.dll или другая учётная запись службы, решает лид; проверка на живом Windows — T-49 |
| Замер скорости идёт из замера по расписанию с адресами из конфигурации сервера (T-13) и из диагностической `vko-agent speed`; против `speedtest` и `ndt7` из `make up` не запускался (docker на машине разработчика не было) — LibreSpeed проверен на подменном HTTP-сервере, ndt7 — на подменном сервере и живым замером против публичного ndt-server M-Lab; методика LibreSpeed отличается от его веб-клиента: без поправки ×1,06 и без «разгона» 1,5 / 3 с, upload считается по байтам, отданным в сокет; ndt7 не ходит через HTTP-прокси, сообщения upload — до 64 КиБ вместо 1 МиБ (иначе pong ждёт за длинным кадром и сервер перестаёт присылать отчёты), download ждёт закрытия теста сервером до 15 с (T-10) | download LibreSpeed примерно на 6 % ниже, чем в браузере, upload на медленной линии чуть выше; на медленной далёкой линии download через ndt7 длится до 15 с вместо 10; в сети школы только с HTTP-прокси резерв ndt7 не сработает | замер против `make up` — до демо, в контрольном прогоне T-58; поправка и «разгон» в методике — решает лид, новым ADR (ADR-012) |
| Очередь замеров `agent/internal/queue/` (SQLite `queue.db` в папке данных) досылается службой после регистрации: при старте, раз в 5 мин, сразу после замера по расписанию (T-13), после восстановления связи по heartbeat (T-12) и после смены сети (T-13); сквозь backend не проверялось (`POST /api/measurements` и `/batch` отвечают 501 до T-15), проверено тестами на подменном сервере и живым прогоном `measure` против него; на живом Windows `queue.db` не создавался — только сборка и `go vet`; ответы 401/403 (заблокированное устройство) считаются временными — повтор раз в час без удаления записей; `Retry-After` больше 24 ч урезается до 24 ч (T-11) | восстановление связи агент замечает очередным heartbeat (по умолчанию раз в 5 мин) или сменой сети, поэтому очередь уходит в пределах интервала heartbeat, а не в ту же секунду; при переполнении старые записи удаляются без отправки — это видно в журнале службы | T-15: сквозная проверка 201/409 с backend; T-49: проверка на Windows |
| Конфигурация с сервера (`GET /api/agent/config` с ETag раз в `config_refresh_interval_s`, кэш последней удачной в `agent-config.json` папки данных) сквозь backend не проверялась: эндпоинт отвечает 501 до T-17, проверено тестами на подменном сервере (200 с ETag, 304 при совпадении `If-None-Match`, применение нового расписания без перезапуска, работа по кэшу при недоступном сервере); пока конфигурации нет ни с сервера, ни в кэше, служба замеров не делает; невалидную конфигурацию агент пишет в журнал и продолжает работать по прежней; поля `thresholds` и `latest_version` ответа агент не использует (T-13) | на чистой установке до T-17 служба только регистрируется и досылает очередь; демо-сценарий 4 (смена расписания и адреса сервера замеров в админке) целиком не проверить | сквозная проверка — после T-17 и T-34; пороги агенту не нужны, оценивает сервер (ADR-004); обновление по `latest_version` — T-50; проверка на живом Windows — T-49 |
| Смена сети определяется опросом поднятых интерфейсов и их адресов раз в 15 с (`netinfo.Watch`), уведомления ОС (`NotifyAddrChange` на Windows, netlink на Linux) не используются; после смены служба ждёт 30 с, делает проверку связи и при связи будит досылку очереди — всё это строками в журнале службы; проверено юнит-тестом на сравнении снимков, на живом Windows не проверялось (T-13) | реакция на смену адаптера — до 15 с + 30 с; короткое мигание линка между опросами агент не заметит; смена сети, пока ПК спал, будет замечена после пробуждения | уведомления ОС — решает лид отдельной задачей; проверка на живом Windows — T-49 |
| Heartbeat и простои (T-12) сквозь backend не проверялись: `POST /api/devices/heartbeat` и `POST /api/outages` отвечают 501 до T-16, проверено тестами на подменном HTTP-сервере (204/201/409/ошибка) и на живом Windows не запускалось — только сборка и `go vet` под `GOOS=windows`; интервал heartbeat берётся из `GET /api/agent/config` (T-13), а пока конфигурации нет — из умолчания 5 мин в `HeartbeatOptions`; простой закрывается только следующим heartbeat, поэтому его конец известен с точностью до интервала; `vko-agent status` открытый простой не показывает; при выключенном ПК дольше двух интервалов конец простоя ставится по последней неуспешной проверке, то есть длинный простой линии в выключенном кабинете занижается; простой, отклонённый сервером как невалидный (422), помечается `rejected` и лежит в `queue.db` до лимита возраста — на сервер он не попадёт, а `vko-agent status` отклонённые не считает; у досылки простоев нет своей паузы: при 403 или 500 на `POST /api/outages` попытка повторяется каждым heartbeat, тогда как у очереди замеров пауза 30 с → 1 ч (ADR-006) | в панели простой появится только после T-16; длительность простоя округляется вверх до интервала heartbeat; сколько простоев ждёт отправки, видно только в `queue.db` и в журнале службы | T-16: приём heartbeat и простоев на сервере и сквозная проверка 201/409; T-49: проверка на живом Windows |
| Код установки и токен устройства начинаются с идентификатора своей строки (`VKO-<id>-<секрет>`, `<device_id>.<секрет>`): argon2-хэш посолен, и найти строку по одному хэшу нельзя (T-14) | идентификатор кода и устройства виден в самом секрете; случайная часть кода — 50 бит, токена — 256 бит | не планируется: без идентификатора пришлось бы перебирать argon2 по всем выданным кодам; ограничение частоты запросов — T-51 |
| У `POST /api/devices/register` нет ограничения частоты, а отказ по коду отличает «не найден» от «просрочен» и «использован» (T-14) | код из 50 бит теоретически перебираем, пока действует 7 дней; удачный перебор даёт устройство в чужой школе | T-51: ограничение частоты и журнал отказов авторизации (ADR-005, ADR-008) |
| argon2 проверяется на каждом запросе агента: 19 МиБ и около 10 мс процессора на запрос, кэша проверенных токенов нет (T-14) | при 1000 устройств и heartbeat раз в 5 мин это примерно 3–4 проверки в секунду; на слабом сервере заметно в нагрузке | T-56: замерить под нагрузкой; кэш проверенных токенов или более дешёвый разбор — решать по результату |
| Выдача кодов установки (`POST /api/devices/enrollment-codes`) и ротация токена — по-прежнему заглушка T-36; коды заводятся только из тестов (`backend/tests/factories.py`) или SQL (T-14) | сквозной сценарий «админ выдал код → школа поставила MSI» из панели пока не пройти | T-36: реализация выдачи кода, блокировки и ротации; тот же `app/core/security.py` |
| Кабинет из параметра установки `ROOM` только выбирает существующую точку мониторинга школы (при совпадении названия кабинета, иначе — главная точка); новая точка не создаётся, тип точки (основная/резервная линия) из мастера MSI в контракт не входит (T-14) | все ПК школы без совпадения по кабинету садятся на главную точку, то есть на основную линию; замеры резервной линии до T-35 привязать некуда | T-35: точки мониторинга в панели; перепривязка устройства — T-36 |
| Повторная регистрация с тем же `device_uid` по новому коду своей школы выдаёт новый токен и сохраняет историю; регистрация не пишется в журнал аудита и не трогает `last_seen_at` (T-14) | потерянный токен лечится новым кодом без второй карточки устройства, но факт перерегистрации виден только по `updated_at` | журнал административных действий и ошибок авторизации — T-20 (ADR-008); `last_seen_at` — T-16 |
| MSI без подписи Authenticode, пока заказчик не даст сертификат | антивирус может ругаться на `VKO-Agent.msi` или блокировать установку | подпись — T-49; сертификат запросить у заказчика |
| SMTP заказчика не настроен | письма обращений и e-mail-уведомления об инцидентах не уходят; обращение сохраняется как «не отправлено», PDF сохраняется | параметры SMTP — от заказчика; отправка обращений — T-48, e-mail-уведомления — T-42 |
| Telegram-бот не заведён | Telegram-уведомления не уходят; остаются панель (SSE) и e-mail при настроенном SMTP | бота заводит команда в T-42 (нужен для демо-сценария 2, plan.md §15); токен — только в окружении |
| Ключ LLM не задан | AI-черновик обращения недоступен, текст пишется вручную | ключ — только в окружении, заводится в T-46 (нужен для демо-сценария 3, plan.md §15); черновик — T-47; Ollama — вариант для закрытого контура |
| Карта — публичные OSM-тайлы на демо | карта зависит от внешнего сервиса; без интернета подложка не грузится | свой tileserver — позже |
| Казахский язык интерфейса в MVP не планируется | панель только на русском | ADR-010; казахский — отдельной волной «позже» |
| VPN не реализуется | канал — HTTPS/TLS 1.2+ через Caddy и токен устройства | WireGuard — по требованию заказчика, отдельной задачей (ADR-010) |
| Готового Linux-пакета агента нет | на Linux агент ставится вручную через `vko-agent install` (T-06), без пакета | T-52, волна v1 |
| Самообновление агента в MVP не реализуется | новая версия агента ставится переустановкой MSI | T-50, волна v1 (п. 20, plan.md §4.6) |
| Аналитика инцидентов в MVP не строится | п. 19 закрыт частично: карточка, статусы и длительность есть (T-41), сводки по количеству, длительности и повторяемости по школам, районам и поставщикам нет | T-45, волна v1 (plan.md §7) |
| Выгрузки формируются синхронно | выгрузки больше 10 000 строк и PDF выполняются в запросе, а не в фоне; списка выгрузок нет | T-33, волна v1 (plan.md §12) |
| Резервное копирование БД не настроено | при потере тома `db` история замеров теряется | T-53 (pgBackRest), волна v1 (plan.md §13) |
| Sentry, Prometheus и Grafana не подключены | ошибки и нагрузка сервера видны только в логах контейнеров | T-54, волна v1 (plan.md §2) |
| Смена конфигурации сервера считается в агенте тем же событием, что и включение ПК: пропущенный слот нового расписания уезжает на случайные 1–15 мин (`CatchUp=true`), а не запускается немедленно. Но `runConfig` зовёт `SetSchedule` на любое изменение конфигурации, а не только расписания, поэтому смена, например, `heartbeat_interval_s` тоже может сдвинуть ещё не наступивший момент текущего слота внутри того же дня (T-13) | замер не теряется и лишнего не возникает, но момент внутри слота может сместиться; замер по пропущенному слоту может пройти уже вне окна слота — как и после включения ПК | отдельной задачей сравнивать в `apply` слоты и часовой пояс и взводить признак только при их смене |
| Если запись открытия простоя в `queue.db` не удалась (ошибка диска), закрытие простоя тихо ничего не находит: `CloseOutage` обновляет строку строго по её `started_at` и не пишет в журнал, что обновлять было нечего (T-12) | редкий простой может не дойти до сервера, и в журнале службы этого не будет видно | логировать `RowsAffected = 0` в `CloseOutage` — отдельной задачей вместе с остальной диагностикой очереди |

Как добавлять строки: если сливаете задачу с недоделкой, добавьте строку сюда — в колонке
«Что делать» укажите номер задачи, которая это доделает (или «не планируется»), — и сошлитесь на
неё из записи в `docs/worklog.md`. Когда доделано — строку удалите в той же ветке, где доделали.
