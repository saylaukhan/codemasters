import { APP_NAME } from './lib/app-info'

// Placeholder root component. Providers, router, Refine and the AntD theme arrive in T-21.
function App() {
  return (
    <main>
      <h1>{APP_NAME}</h1>
    </main>
  )
}

export default App
